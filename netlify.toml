[build]
  command = "npm run build"
  publish = "dist"

[context.develop]
  command = "npm run build"
  publish = "dist"

[template.environment]
VITE_ANTHROPIC_API_KEY="Add your Anthropic API key here"
